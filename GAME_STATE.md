# Статус реализации проекта (GAME_STATE)

## ✅ Работает
Системы и модули, которые полностью реализованы и функционируют без заглушек:

1. **Сетевой клиент-серверный обмен:**
   - Передача данных по WebSocket сжатых с помощью `zlib` (уровень 1) и сериализованных в `MsgPack` (реализовано в [websocket.py](file:///c:/Users/Engineer/Desktop/snake%20AI/backend/app/api/websocket.py) и [WebSocketClient.ts](file:///c:/Users/Engineer/Desktop/snake%20AI/frontend/src/worker/WebSocketClient.ts)).
   - Поддержка ping-pong запросов для измерения задержки (ping) сети.
   - Автоматический реконнект клиента с экспоненциальной задержкой.
   - Delta-обновления состояния: сервер передает только измененные данные игроков и новые/съеденные элементы еды, клиент накапливает состояние в [DeltaDecoder.ts](file:///c:/Users/Engineer/Desktop/snake%20AI/frontend/src/worker/DeltaDecoder.ts).
   - Автоматический сброс на FULL-стейт при frame skipping (пропуске кадров в очереди отправки во избежание лагов), гарантирующий быструю синхронизацию клиента при восстановлении нормальной передачи.

2. **Клиентская интерполяция и предсказание:**
   - Интерполяция кадров в [StateInterpolation.ts](file:///c:/Users/Engineer/Desktop/snake%20AI/frontend/src/worker/StateInterpolation.ts)## 🟢 Успешные механики (Stable)
- Синхронизация кадров (Worker -> Main)
- Интерполяция камеры и AoI
- Базовая физика и рост
- Тороидальное оборачивание карты
- Передача геометрии через SharedArrayBuffer (с фоллбеком)
- Инкрементальная сетка коллизий (O(1) update)
- Механика смерти: Экран "Потрачено" и надгробия (Tombstones). Исправлено отображение надгробий (размер, frustum culling) и центрирование никнеймов прямо на камне.
- Исправлена логика кликов по интерфейсу на мобильных устройствах.
- Сглажена интерполяция сплайнов змеек при росте.
- Исправлено мгновенное перерождение при попадании в черную дыру.
- Внедрены защитные механизмы от падения сервера (Фаза 1): Инициализация атрибутов state.py при старте, защита порталов при обновлении конфига, проверка мертвых змеек в физике и росте, предотвращение зависания воркера (try-catch и timeout кадров).
- Добавлена защита от зависаний (TypeError) при рассинхронизации змейки: добавлена фильтрация `newBody` от undefined/invalid координат в `DeltaDecoder.ts`, валидация головы игрока при отрисовке на миникарте в `Minimap.ts`, а также безопасные проверки точек сплайнов в `SplineComputer.ts`.
- Устранены XSS-уязвимости в KillFeed и LoginScreen, а также закрыт чит-вектор на накрутку очков через WebSocket SCORE.
- Значительные оптимизации производительности (Фаза 2): Оптимизирована миникарта (O(N) вместо O(N^2) с 45,000 вызовами), устранены утечки памяти и GC спайки в Worker'е (через преаллокацию Maps для Food, BlackHoles, Portals), пред-аллоцированы GPU-буфера для змеек `SnakeRenderer` (600,000+ вершин для предотвращения переаллокаций) и оптимизирован рендеринг никнеймов `NicknameRenderer` с GPU-ускорением (translate3d).
- Полный рефакторинг тороидальной математики и логики (Фаза 3): Единая реализация тороидальной математики на клиенте (`MathUtils.ts`) и сервере (`math_utils.py`). Устранены лаги при пересечении границ карты, исправлено двойное сглаживание камеры (`CameraPredictor.ts`) и внедрен динамический тороидальный split сплайнов змеек (`SplineComputer.ts`) с корректным разделением сегментов при прохождении через порталы (предотвращающим визуальное натягивание тела змейки по прямой линии между входным и выходным порталами).
- Исправление багов коллизий и телепортации (Фаза 3): Внедрена корректная очистка и добавление в сетку коллизий во время телепортации змейки через порталы (`teleportation.py`), предотвращающая прохождение других игроков сквозь телепортирующихся. Также исправлено использование `bh_manager` в `world_elements.py` для корректного размещения элементов мира.
- Архитектурный рефакторинг (Фаза 4): Избавление от хардкодов констант (вынос `CELL_SIZE` в `game_config.py`), удаление мертвого кэша `foodMap` из воркера для снижения нагрузки, исправление утечек памяти WebGL (двойной `dispose()` для shared-геометрии) в `FoodRenderer`, `PortalRenderer`, `BlackHoleRenderer`.
- UX и Управление (Фаза 5): Исправлено "залипание" управления при переключении мышь/клавиатура. Уменьшена задержка интерполяции (размер буфера снижен с 20 до 4 кадров = 100-130ms вместо 666ms). Улучшено мобильное управление: введена слепая зона (5% экрана) и исправлены случайные нажатия зоны ускорения поверх UI-элементов. Прямое гравитационное притяжение к черным дырам без бага в 45°. В меню настроек добавлена кнопка включения "Keyboard".
- Архитектурный рефакторинг и оптимизация памяти (Фаза 6):
  - Оптимизировано построение пространственной сетки еды `food_grid`: теперь она строится ровно один раз за тик в `game.py` и переиспользуется в системах гравитации (`gravity.py`) и поедания (`food_eating.py`).
  - Коллизионная сетка перестраивается с нуля каждый тик в `collision.py`, полностью решая проблему "призрачных" коллизий.
  - Устранен костыль с MsgPack Placeholder — словари кэшируются напрямую в JSON-подобном виде.
  - Модуль `state.py` разделен: логика видимости (AoI) вынесена в `visibility.py`, логика дельта-сериализации — в `serialization.py`.
  - Тело игрока (`body`) переведено на плоскую структуру `deque[float]` (координаты `[x0, y0, x1, y1...]`), что привело к 10-кратному снижению объема выделяемой памяти под координаты и повышению производительности.
- Оптимизация производительности: мемоизация списков `body` и `new_heads` в `entities.py` внутри метода `to_dict()`, исключающая лишние O(n) аллокации массивов координат при каждой сериализации кадра.
- Рефакторинг Admin API (Фаза 7): Перевод эндпоинтов на использование `APIRouter` с `Depends(require_admin)` в `server.py`, удаление ручного вызова `require_admin()` из обработчиков в `admin.py`. Устранена Timing Attack уязвимость при проверке пароля администратора (`hmac.compare_digest`).
- Оптимизация производительности сервера (Фаза 8): Вынесена функция `process_client` из `game_loop` в `server.py` для предотвращения пересоздания объекта на каждый тик. Сжатие `zlib.compress` переведено на асинхронное выполнение через `asyncio.to_thread` во вспомогательном потоке, чтобы разгрузить основной event loop от CPU-интенсивной задачи.
- Исправлен критический баг (crash) при усыхании змейки ниже минимальной длины в `growth.py`.
- Оптимизация производительности: избавление от O(n) операций вставки в начало списка `insert(0)`. В `physics.py` и `teleportation.py` заменено на `append` с последующим разворотом списка `reverse()` перед сериализацией (`serialization.py`), что сохраняет правильный порядок координат с O(1) добавлениями.
- Оптимизация производительности: устранены O(N) циклы проверки дистанции при входе и выходе из порталов (`teleportation.py`). Теперь расстояние рассчитывается только для хвоста змейки O(1), так как змейка жестко следует по своему пути.
- Внедрена очередь ввода (input queue) в state.py, websocket.py и server.py для предотвращения race condition при мутации состояния игры во время обработки WebSocket-сообщений.
- Добавлена защита от переполнения счетчика еды (`food_id_counter` в `food_manager.py`): теперь значение берется по модулю 2_000_000_000, что предотвращает ошибки размера int в MsgPack при длительном аптайме сервера.
- Устранена утечка asyncio задач при перезапуске сервера через API админки (очистка и отмена `pending_disconnects` в `admin_restart_game`).
- Оптимизация сборщика мусора (GC) в `collision.py`: предотвращено ежетиковое создание пустых списков для `tombstones`, когда массив пуст, что снижает нагрузку на GC. Устранены дублирующиеся циклы и проверки `player.is_dead` за счет фильтрации активных игроков один раз за тик.
- Оптимизация сборщика мусора (GC) воркера клиента: устранена ежекадровая аллокация `Float32Array` в `_buildFoodMinimap` за счет использования переиспользуемого `GrowableFloat32Array`. В `FrameComputer.ts` убран дорогостоящий spread-оператор `...state` при формировании кадра, и оптимизирована передача массивов еды, порталов и черных дыр (zero-copy через добавление буферов в `transferables`), что значительно снижает GC-нагрузку.
- Ограничены данные, возвращаемые `/health` эндпоинтом: теперь он возвращает только `{"status": "ok"}` (предотвращена незначительная утечка информации об онлайне).
- Оптимизация производительности: Внедрено глобальное кэширование парсинга формул роста (`formula_parser.py` и `FormulaParser.ts`), предотвращающее пересоздание и ре-эвалюацию регулярных выражений на каждое вычисление.
- Рефакторинг UI (Фаза 9): Статические inline-стили из `main.ts` перенесены в соответствующие CSS-классы (`main.css` и `hud.css`), улучшая читаемость кода и разделение логики и стилей.
- Очистка логики: Удален нерабочий мертвый код предсказания скорости (dead reckoning) в `CameraPredictor.ts`, значения которого безусловно перезаписывались серверными координатами.
- Оптимизация коллизий надгробий (Tombstone Ghost Time): Добавлен период неуязвимости (1.5 сек) для только что появившихся надгробий. Это решает баг "двойной смерти", когда змейка-победитель сразу же разбивалась об надгробие поверженного врага. Также уменьшен коллизионный радиус надгробий с `3.0` до `1.2` (в масштабах ячеек), чтобы полностью соответствовать их реальному 3D-размеру (`1.2 * gridSize` на клиенте) и исправить ощущение невидимого препятствия.
- Синхронизирована максимальная длина никнейма: в LoginScreen.ts атрибут maxlength установлен в 16 в соответствии с бэкенд-константой MAX_NAME_LENGTH.
- Обновлен HUD: история получения очков (.score-feed) и индикатор пинга (.hud-ping) вынесены из контейнера счета (.score-panel) напрямую в родительский контейнер HUD (.hud-container) во избежание проблем позиционирования из-за backdrop-filter.
- История получения очков (.score-feed) на десктопе перемещена в нижний левый угол (position: absolute; left: 20px; bottom: 20px;).
- Отключена функция сворачивания таблицы лидеров на десктопе (теперь она всегда открыта, стрелка сворачивания скрыта, клик отключен).
- Иконка триггера бокового меню (настроек) заменена на бургер-меню (☰) вместо шестеренки (⚙️) на десктопе.
- Иконка бургер-меню на десктопе смещена чуть ниже и левее для визуального выравнивания справа от заголовка топ 10.
- Пинг перемещен в самый угол экрана на десктопе (position: absolute; right: 20px; bottom: 10px; z-index: 105;), накладываясь поверх прозрачной угловой области круглого радара, с выравниванием по правому краю.
- Для лучшего контраста индикатору пинга на десктопе добавлена черная обводка (text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000).
- Ширина окна текущего счета (.score-panel) уменьшена в 2 раза на десктопе (первая колонка сетки HUD уменьшена с 300px до 150px).
- Размер шрифтов счетчика счета уменьшен примерно в 1.5 раза на десктопе: .score-title уменьшен с 14px до 10px, .score-value — с 36px до 24px.
- Исправлена ошибка 404 при прямом переходе на /admin в продакшене: добавлена поддержка хэш-роутинга (переход по адресам /#admin или /?admin работает "из коробки" на любом сервере без изменения его настроек).
- Исправлена загрузка пароля администратора в деве: в start_dev.bat добавлено чтение переменных из .env (теперь пароль "povelitel-polzunov" корректно работает как в деве, так и в проде).
- Устранена проблема CORS (Failed to fetch) при доступе к админ-панели по локальным IP (например, 192.168.3.38:5173): в дев-режиме бэкенда теперь разрешены запросы с любых хостов (allow_origin_regex="https?://.*").
- В админ-панели заменен розовый акцентный цвет (var(--accent-pink)) на единый устоявшийся красный акцентный цвет игры (var(--accent) / #e63946) во всех элементах (заголовки, активные кнопки табов, тултипы при наведении).
- Фикс фокуса инпутов: Восстановлено состояние фокуса в админке, чтобы при вводе названия SVG-файла (`image`) инпут не сбрасывался после каждого символа.
- Исправлено цветовое оформление лейблов: слова "Server:" в админ-панели и "Ping:" в игровом HUD теперь отображаются нейтральным серым цветом (var(--text-muted)), а статусное выделение (зеленый/желтый/красный) применяется только к самим значениям (online/offline и задержке в мс).
- Внедрение настоящих 3D-теней и оптимизация рендеринга (Фаза 10):
  - Включен `PCFShadowMap` в `SceneManager` и добавлен невидимый `ShadowMaterial` для физически корректного отбрасывания теней на поверхность (устранено предупреждение о депрекации `PCFSoftShadowMap`).
  - Направленный источник света (`DirectionalLight`) перенесен непосредственно над камерой (с небольшим смещением на 0.1 для исключения сингулярностей матриц в Three.js), что убирает сдвиг теней в сторону.
  - Настроен радиус размытия теней (`dirLight.shadow.radius = 6`), делая их мягкими, рассеянными и похожими на затенение Ambient Occlusion.
  - Динамический размер усеченного конуса теней (`shadow.camera.left/right/top/bottom`) теперь синхронизирован с радиусом тумана (`msg.fogRadiusWorld`), оптимизируя область рендеринга теней в соответствии с зоной видимости.
  - Добавлено сферическое 3D-затенение и Ambient Occlusion для частиц еды (`food.glsl.ts`), делая их похожими на глянцевые объемные жемчужины.
  - Еда опущена по оси Z в 2 раза (Z-offset уменьшен с 1.5 до 0.75 в воркере/клиенте).
  - Переведен материал надгробий (`TombstoneRenderer.ts`) с плоского `MeshBasicMaterial` на полноценный `MeshStandardMaterial`, благодаря чему камни корректно освещаются источниками света с красивыми 3D-гранями и тенями.
  - Геометрия еды переведена на `CircleGeometry` для круглых теней.
  - Полностью вырезана огромная старая логика "фейковых" теней, рассчитываемых на CPU воркера (массивы `shadowBufs`, `tempFoodShadowMatrices`, передача их в Main Thread, шейдеры `snakeShadow.glsl` и `foodShadowShader`). Это радикально снизило нагрузку на CPU, память и шину передачи данных.
  - Исправлено отображение змеек, еды, порталов и черных дыр: отключен `logarithmicDepthBuffer` в `SceneManager.ts` (поскольку он несовместим с кастомными `ShaderMaterial` без инъекции специальных фрагментов, вызывая их исчезновение).
  - Исправлена отрисовка тела змейки во всех 9 копиях тороидального мира: в vertex shader `snakeBody.glsl.ts` добавлено корректное умножение вершинных координат на `instanceMatrix`.
  - Исправлен баг "невидимого хвоста" при пересечении границы: удалена устаревшая логика `discard` в шейдере `snakeBody.glsl.ts`, которая некорректно обрезала (клипала) части змеи, вышедшие за пределы базовой `[0, width]` карты. Теперь 9 инстансов отрисовываются без обрезаний.
  - Добавлено псевдослучайное вращение частиц еды (`FoodComputer.ts`) для более естественного визуального отображения, реализованное через вращение 2D-базиса матриц инстансов (по оси Z).
  - Включен аппаратный MSAA-антиалиасинг (Multi-Sample Anti-Aliasing, `samples: 4`) для оффскрин-рендеринга в `PostProcessing.ts`, что полностью устраняет визуальные "лесенки" и зубчатые края во время активных постэффектов (черных дыр).
  - Внедрен плоский "Туман Войны" в плоскости земли (мировом пространстве): вместо багованного пост-процессинга используется гигантский полупрозрачный шейдерный `FogMesh` в `SceneManager`, который располагается поверх сцены и плавно скрывает удаленные объекты на основе их истинного тороидального расстояния до головы змейки.
- Оптимизация Minimap: Удален рендеринг в 9 копиях (циклы [-1, 0, 1]) для иллюзии тороидального мира в `Minimap.ts`, теперь элементы рисуются только один раз по их прямым координатам. Также в `FrameComputer.ts` добавлена фильтрация мелкой еды (value < 10.0) из данных миникарты для радикального снижения количества отрисовываемых точек.
- Оптимизация производительности бэкенда: полностью удалена O(N) логика сетки игроков (`player_grid`) из `state.py`, `serialization.py` и `visibility.py`. Теперь проверка видимости клиентов (AoI) выполняется быстрым прямым итерированием по словарю игроков с расчетом дистанции через `toroidal_distance`.
- Визуальные улучшения черных дыр: Добавлено мягкое градиентное затемнение фона и оранжевый эффект свечения (glow) от центра (в `blackHole.glsl.ts`), реалистично освещающий близлежащие объекты без необходимости добавления новых сеток.
- Внедрение `RenderConfig.ts` (Фаза 11): Централизована система управления слоями отрисовки (`RenderLayer`), материалами и тенями. Разведены `renderOrder` для Змейки (3, 4, 5), Порталов (6, 7, 8) и Черных Дыр (9, 10, 11), что навсегда устранило скрытые конфликты глубины (Z-fighting). Удален хардкод из всех рендереров.
- Исправление рендеринга на границах карты и синхронизации телепортации (Фаза 12):
  - Координаты глаз змеек (`SnakeProcessor.ts`), никнеймов, еды (`FoodComputer.ts`), порталов (`PortalComputer.ts`) и черных дыр (`BlackHoleComputer.ts`) теперь автоматически проецируются (оборачиваются) на ближайшую виртуальную тороидальную копию карты относительно камеры, устраняя исчезновение элементов и иллюзию отсутствия коллизий при переходе через край карты.
  - Исправлен баг зависания состояния телепортации `teleport_state` на клиенте при выходе змейки из портала в `DeltaDecoder.ts` (поле принудительно сбрасывается в `"none"` при отсутствии в дельта-кадре).
- Оптимизация многопользовательского режима и интерполяции (Фаза 13):
  - Увеличен размер очереди `StateInterpolation.ts` с 4 до 20 кадров, что полностью устранило статтеры и зависания чужих змеек при сетевом джиттере.
  - Внедрен Frustum Culling в `SnakeProcessor.ts`: теперь сплайны и буферы рассчитываются только для змеек, попадающих в зону видимости локального игрока.
  - Устранены ежекадровые аллокации тысяч массивов и вызовов GC за счет добавления `WeakMap` в `FoodComputer.ts` и отказа от spread-операторов в `DeltaDecoder.ts`.
  - Ускорены серверные расчеты AoI в `visibility.py` и `serialization.py` за счет инлайнинга формул расчета дистанций.
  - Устранены периодические "мертвые" фризы браузера (Major GC pauses): медленный и ресурсоемкий асинхронный `DecompressionStream` заменен на сверхбыстрый синхронный `pako.inflate` для распаковки пакетов, а `parsePoints` переписан с преаллокацией памяти.
  - Добавлена защита воркера от лавины устаревших запросов кадров: если запрос кадра отстает от момента отправки более чем на 100 мс, воркер сбрасывает расчеты и мгновенно возвращает пустой кадр.
- Оптимизация сборщика мусора (GC) воркера клиента (Фаза 14): `Player.body` переведено на использование плоских массивов `number[]` вместо объектов `{x, y}[]`. `DeltaDecoder.ts`, `CameraPredictor.ts`, `SnakeProcessor.ts` и `SplineComputer.ts` обновлены для работы с плоскими массивами по прямым индексам. Это устраняет аллокацию тысяч мелких объектов `{x, y}` при декодировании каждого кадра с сервера, предотвращая микро-фризы браузера при сборке мусора.
- Интеграция процедурной 3D-модели вишни: вместо `cherry.svg` еда с индексом 1 теперь использует процедурную 3D-геометрию из `CherryGeometry.ts` с материалом `MeshStandardMaterial` и vertexColors, что делает самую частую еду полностью трехмерной.
  - Создана процедурная 3D-геометрия винограда (`GrapeGeometry.ts`), готовая к интеграции в рендерер.
  - Создана процедурная 3D-геометрия персика (`PeachGeometry.ts`), готовая к интеграции в рендерер.
- Создана процедурная 3D-геометрия клубники (`StrawberryGeometry.ts`), готовая к интеграции в рендерер.
3. **Физика движения и управление:**
   - Движение змеек, расчет поворотов и ускорения с потерей очков в [physics.py](file:///c:/Users/Engineer/Desktop/snake%20AI/backend/app/engine/systems/physics.py) и [boost.py](file:///c:/Users/Engineer/Desktop/snake%20AI/backend/app/engine/systems/boost.py).
   - Поддержка клавиатурного ввода, руления мышью и мобильного тач-управления в [InputManager.ts](file:///c:/Users/Engineer/Desktop/snake%20AI/frontend/src/game/InputManager.ts) и [MobileControls.ts](file:///c:/Users/Engineer/Desktop/snake%20AI/frontend/src/ui/MobileControls.ts). Поддерживается мультитач-управление на мобильных устройствах, предотвращающее залипание и зависание ввода.
   - Внедрен троттлинг непрерывного ввода на клиенте до 40 мс (со snap-to-zero) и оптимизирована отправка ускорения (edge-triggered). На сервере исправлен алгоритм скользящего окна рейт-лимитера (до 50 сообщений в секунду), предотвращающий "залипание" змейки из-за сетевого джиттера.

4. **Система пространственного деления (Spatial Partitioning):**
   - Оптимизация коллизий, притяжения и поедания еды на сервере с помощью разделения карты на сетку с размером ячейки `CELL_SIZE = 10.0` (в `collision.py`, `food_eating.py`, `gravity.py`, `serialization.py`).
   - Черные дыры накапливают очки поглощенной еды и при своем исчезновении выбрасывают накопленную массу обратно на арену.

5. **Отрисовка 3D-графики (Three.js):**
   - Рендеринг тела змейки через кастомный шейдер [snakeBody.glsl.ts](file:///c:/Users/Engineer/Desktop/snake%20AI/frontend/src/renderer/shaders/snakeBody.glsl.ts) с поддержкой 5 скинов (Solid, Zebra, Tiger, Cyberpunk, Rainbow) и flat contour edge обводкой.
   - Инстансированный рендеринг еды, порталов, черных дыр и частиц хвоста для высокой производительности.
   - Бесконечный пол с сеткой ([ground.glsl.ts](file:///c:/Users/Engineer/Desktop/snake%20AI/frontend/src/renderer/shaders/ground.glsl.ts)).
   - Эффект гравитационного линзирования черных дыр и плоского тумана войны в мировых координатах через пост-обработку ([PostProcessing.ts](file:///c:/Users/Engineer/Desktop/snake%20AI/frontend/src/renderer/PostProcessing.ts)), отслеживающий голову змейки.
   - Отображение ников игроков в виде HTML-оверлеев над головами змеек (кроме самой змейки локального игрока). Никнейм локального игрока скрыт для предотвращения визуального загромождения.
   - Исключительно 3D-режим отображения и следования камеры; 2D-режим и переключатели удалены из всех модулей игры.

6. **Система телепортации (FSM):**
   - Перемещение змеек через порталы с 5 состояниями конечного автомата (entering, in_transit, exiting и др.), задержкой телепортации и блокировкой повторного входа.

7. **Панель Администратора:**
   - Полноценную REST/WebSocket панель управления в [AdminPanel.ts](file:///c:/Users/Engineer/Desktop/snake%20AI/frontend/src/admin/AdminPanel.ts) и [admin.py](file:///c:/Users/Engineer/Desktop/snake%20AI/backend/app/api/admin.py).
   - Редактирование всех параметров конфигурации в реальном времени, валидация математических формул роста на сервере через AST-дерево.
   - Canvas-симулятор распределения еды по алгоритму Box-Muller в [FoodSimulator.ts](file:///c:/Users/Engineer/Desktop/snake%20AI/frontend/src/admin/FoodSimulator.ts).
   - Удаленный перезапуск сервера с закрытием соединений.

8. **Система калибровки наклона (Tilt Controls):**
   - Управление гироскопом ([InputManager.ts](file:///c:/Users/Engineer/Desktop/snake%20AI/frontend/src/game/InputManager.ts)) автоматически калибрует нулевую точку наклона в момент активации режима для комфортной игры в любом положении телефона.

---

## 🔴 Сломано или не реализовано
Ошибки, отсутствующие файлы, несоответствия карты проекта и неиспользуемый код:

1. **Несуществующий файл конфигурации Vite:**
   - В [PROJECT_MAP.md](file:///c:/Users/Engineer/Desktop/snake%20AI/PROJECT_MAP.md#L13) указан файл `frontend/vite.config.ts`, однако в каталоге `frontend/` этот файл отсутствует. Проект использует настройки Vite по умолчанию.
2. **Неиспользуемый слушатель пинга в UI:**
   - В файле [main.ts](file:///c:/Users/Engineer/Desktop/snake%20AI/frontend/src/main.ts#L104) содержится пустая заглушка, предназначенная для будущего внедрения системы мониторинга сети:
     ```typescript
     window.dispatchEvent(new CustomEvent("game-ping", ...));
     ```

---

## 🟡 Частично реализовано / WIP
Компоненты с неполной реализацией или логическими допущениями:

*В данный момент критичных проблем в WIP не обнаружено.*

---

## ⚠️ Связанные риски
Потенциальные проблемы и неочевидные побочные эффекты в логике:

* Возможный кратковременный всплеск сетевого трафика и нагрузки на CPU при частых FULL-синхронизациях из-за постоянного пропуска кадров (при критически медленном интернет-соединении у клиента).
* Все остальные обнаруженные ранее риски (monkeypatching, зависание порталов, десинхронизация формул, невидимые препятствия от трупов, отсутствие стилей экрана смерти) были устранены.
